// 2. Write a function that takes a string as input and returns the string in uppercase.
let str = "sibasish";
function toUpperCaseStr(){
    let newstr = str.toUpperCase();
    console.log(newstr);
    
}
toUpperCaseStr();