// 5. Check if String Contains Substring, Use a method to check if "Hello world" contains "world".
let str ="Sibasish";
let subStr="Siba";
if(str.includes(subStr)){
    console.log(str+" contains "+subStr);
}
else{
    console.log(str+"does not contain"+ subStr);
    
}
